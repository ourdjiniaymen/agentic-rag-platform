services:
  backend:
    command: >
      python -Xfrozen_modules=off
      -m debugpy
      --listen 0.0.0.0:5678
      --wait-for-client
      -m uvicorn app.main:app
      --host 0.0.0.0
      --port 8001
      --reload
    ports: !override
      - "8001:8001"
      - "5678:5678"
